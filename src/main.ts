// Main entry point for SASSC Location CRM System

function initializeApp() {
  console.log('SASSC Location CRM System initialized');
}

document.addEventListener('DOMContentLoaded', () => {
  initializeApp();
});

export { initializeApp };
